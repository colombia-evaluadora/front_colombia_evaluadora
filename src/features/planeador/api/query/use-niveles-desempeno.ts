/** Nombres por default — lo que se ve si `useUnidadValoracionesQuery`
 *  (`GET /planeador/unidades/:id/valoraciones`, la fuente real de la tabla
 *  "Criterios de la unidad" y de `DialogAgregarCriterio`) todavía no
 *  resolvió. Orden de menor a mayor desempeño, igual que las bandas de una
 *  escala real ordenadas por `notaMinima`. */
export const NIVELES_DESEMPENO_DEFAULT_NOMBRES = ["Bajo", "Básico", "Alto", "Superior"]
