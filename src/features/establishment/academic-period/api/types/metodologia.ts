// Opción de metodología tal como la entrega el backend: `key` es el valor que
// se guarda/manda, `label` el texto visible en el select.
export interface MetodologiaOption {
  key: string
  label: string
}
