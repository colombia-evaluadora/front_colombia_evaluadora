// Opción de nodo curricular tal como la entrega el backend: `key` es el valor
// que se guarda/manda, `label` el texto visible en el select.
export interface CurriculumNodeOption {
  key: string
  label: string
}
