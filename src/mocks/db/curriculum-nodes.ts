// Catálogo de nodos curriculares. Simula lo que en producción entrega el
// backend, de modo que el front no hardcodee las opciones del select.
export const curriculumNodesDb: string[] = ["AS", "AR"]